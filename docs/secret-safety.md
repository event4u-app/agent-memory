# Secret safety

`agent-memory` enforces a **reject-by-default** policy for secrets, PII,
and environment-value leakage at every ingress path — CLI, MCP,
service layer, logger output, and the embedding boundary.

This document describes:

- What the system considers a secret and how it is detected.
- How detection interacts with the configured policy (`reject` |
  `redact`).
- Which patterns are in the catalog today (table below — auto-generated).

## How detection works

Every string that would be stored, logged, or shipped to an embedding
provider is scanned by `scanForSecrets` (see
[`src/security/secret-guard.ts`](../src/security/secret-guard.ts)).
The scanner iterates the pattern catalog below in order; specific
patterns are reported before the generic heuristics so the detection
name is the most informative available.

In addition, a Shannon-entropy check flags any quoted alphanumeric
run of ≥ 20 characters whose entropy exceeds `4.0` — catches
ad-hoc tokens that do not match any named format.

## Policy

`config.security.secretPolicy` controls what happens when a detection
fires:

| Policy | Behavior |
|---|---|
| `reject` (default) | Request is rejected with a structured `SecretViolation` error. The payload never contains the secret value — only pattern name, code, and byte offsets. |
| `redact` | The detection is logged as an audit event, the input is scrubbed (`[REDACTED:<pattern>]`), and the operation proceeds with the clean copy. |

Errors follow the
[`secret-violation-v1`](../tests/fixtures/retrieval/secret-violation-v1.schema.json)
schema and are stable across releases.

## Catalog

<!-- GEN:SECRET-PATTERNS:START -->

## Pattern catalog (generated)

This section is regenerated by `npm run docs:secrets` from
[`src/security/secret-patterns.ts`](../src/security/secret-patterns.ts).
If this table and the catalog disagree, the catalog wins — CI fails on drift.

**Catalog version:** `1.0.0` · 
**Total patterns:** 27 · 
**Providers:** 17

| Provider | Name | Code | Confidence | Description | Known false positives |
|---|---|---|---|---|---|
| `anthropic` | `anthropic_api_key` | `SECRET_DETECTED` | high | Anthropic API key (sk-ant-…). | — |
| `aws` | `aws_access_key` | `SECRET_DETECTED` | high | AWS access key id (AKIA…). | — |
| `azure` | `azure_storage_connection_string` | `SECRET_DETECTED` | high | Azure storage connection string with AccountKey. | — |
| `bitbucket` | `bitbucket_access_token` | `SECRET_DETECTED` | high | Bitbucket repository/workspace access token (ATCTT…). | — |
| `digitalocean` | `digitalocean_pat` | `SECRET_DETECTED` | high | DigitalOcean personal access token (dop_v1_…). | — |
| `gcp` | `gcp_api_key` | `SECRET_DETECTED` | high | Google Cloud API key (AIza…). | — |
| `generic` | `basic_auth_url` | `SECRET_DETECTED` | high | Basic auth embedded in a URL (`https://user:pass@host`). | — |
| `generic` | `jwt` | `SECRET_DETECTED` | high | JSON Web Token (three base64url segments). | — |
| `generic` | `connection_string` | `SECRET_DETECTED` | high | Database connection string (postgres://, mysql://, redis://, mongodb://). | — |
| `generic` | `private_key_pem` | `SECRET_DETECTED` | high | PEM-encoded RSA/EC/DSA/PKCS8 private key. | — |
| `generic` | `generic_key_token_secret` | `SECRET_DETECTED` | medium | `api_key=…` / `token=…` / `bearer …` style assignment with a 16+ char value. | documentation examples; placeholder values longer than 16 chars |
| `generic` | `email` | `PII_DETECTED` | high | Email address. | — |
| `generic` | `env_var_value` | `ENV_VALUE_DETECTED` | medium | Standalone `KEY=value` line that looks like a .env entry. | shell export snippets; configuration examples |
| `github` | `github_token` | `SECRET_DETECTED` | high | GitHub personal access / OAuth / user / server / refresh token (ghp_, gho_, ghu_, ghs_, ghr_). | — |
| `gitlab` | `gitlab_personal_token` | `SECRET_DETECTED` | high | GitLab personal access token (glpat-…). | — |
| `gitlab` | `gitlab_deploy_token` | `SECRET_DETECTED` | high | GitLab deploy token (gldt-…). | — |
| `heroku` | `heroku_api_key` | `SECRET_DETECTED` | high | Heroku API key (HRKU-…). | — |
| `npm` | `npm_token` | `SECRET_DETECTED` | high | npm publish/automation token (npm_…). | — |
| `openai` | `openai_api_key` | `SECRET_DETECTED` | high | OpenAI API key (sk-… or sk-proj-…). | — |
| `openssh` | `openssh_private_key` | `SECRET_DETECTED` | high | OpenSSH-format private key. | — |
| `sendgrid` | `sendgrid_api_key` | `SECRET_DETECTED` | high | SendGrid API key (SG.xxx.yyy). | — |
| `slack` | `slack_token` | `SECRET_DETECTED` | high | Slack bot/user/app token (xoxb-, xoxp-, xoxa-, xoxr-, xoxs-). | — |
| `stripe` | `stripe_live_secret_key` | `SECRET_DETECTED` | high | Stripe live secret key (sk_live_…). | — |
| `stripe` | `stripe_restricted_key` | `SECRET_DETECTED` | high | Stripe restricted key (rk_live_…). | — |
| `stripe` | `stripe_webhook_secret` | `SECRET_DETECTED` | high | Stripe webhook signing secret (whsec_…). | — |
| `stripe` | `stripe_live_publishable_key` | `SECRET_DETECTED` | medium | Stripe live publishable key — not secret alone, often leaked alongside the secret key. | public keys in client bundles |
| `twilio` | `twilio_account_sid` | `SECRET_DETECTED` | medium | Twilio Account SID (AC + 32 hex). Paired with an auth token in leaks. | UUIDs prefixed with AC |

Patterns with `confidence: medium` may be neutralised by the allow-list
(roadmap II4) in contexts where the match is demonstrably a non-secret
(UUIDs, commit SHAs, public content hashes).
<!-- GEN:SECRET-PATTERNS:END -->

## Extending the catalog

1. Add an entry to
   [`src/security/secret-patterns.ts`](../src/security/secret-patterns.ts).
   Every entry needs a `name`, `provider`, `code`, `confidence`,
   `regex` (with the `g` flag), and a short `description`.
2. Add a positive + negative test case to
   [`tests/unit/secret-patterns.test.ts`](../tests/unit/secret-patterns.test.ts).
3. Run `npm run docs:secrets` to refresh the table above. CI fails on
   drift between the catalog and this document.

## Policy floor for shared / team-memory deployments

The pattern catalog above is the **technical floor** — what the package itself refuses to ingest. When `agent-memory` is deployed as a shared team brain (see `agents/roadmaps/team-memory-deployment.md`), the **policy floor** is wider: some classes of content the catalog cannot reliably detect must still never enter the shared store.

These are team agreements, not package-enforced rules. They are listed here so any reviewer, contributor, or auditor has one canonical reference.

| Class | Why it stays out | Even if the technical floor passes |
|---|---|---|
| End-customer PII (names, emails, phone numbers, addresses) | The brain is for engineering knowledge, not customer data. GDPR / contractual obligations apply. | Yes — names and emails are not in the secret catalog. |
| Customer-specific identifiers (account IDs, contract numbers, invoice numbers) | Same as above; can be cross-referenced back to PII. | Yes. |
| Production database snippets, query results, or logs containing real user rows | Same as above; bypasses the technical floor when quoted as text. | Yes — quoted text is not scanned for PII. |
| Full URLs to internal admin / staging tooling with embedded session tokens | Tokens may rotate before scanning catches them; the URL itself reveals infrastructure. | Sometimes — only if the token matches a known pattern. |
| Personal opinions about teammates, customers, or vendors | Not engineering knowledge; no operational value in shared retrieval. | Yes — sentiment is not scanned. |
| Drafts of legal, HR, or financial communications | Out of scope for an engineering knowledge base. | Yes. |

What **is** appropriate for shared memory:

- Architecture decisions, domain rules, coding conventions, refactoring notes (per ADR-0001 Decision 3).
- Bug patterns expressed as **shape** (the symptom, the fix, the file/symbol context) — never including real user data that triggered the bug.
- Integration constraints, deployment warnings, test strategies.

When in doubt: **propose, do not promote** (`memory propose` keeps the entry quarantined and visible only to the author until `memory promote` runs — see ADR-0006). The promotion step is the privacy boundary; pause there if the entry's content is borderline.
