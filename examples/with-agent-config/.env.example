# Copy to .env before running `docker compose up -d`.
# Values here are dev-only and match docker-compose.yml.

DATABASE_URL=postgresql://memory:memory_dev@postgres:5432/agent_memory
LOG_LEVEL=info
REPO_ROOT=/workspace
